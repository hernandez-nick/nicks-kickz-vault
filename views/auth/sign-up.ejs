<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
</head>

<body>

    <%- include('../partials/_navbar.ejs', { title: "Sign Up" }) %>

    <main class="main-content auth-shell">
        <section class="form-card auth-card">
            
            <h1 class="page-title">Create Account</h1>
            <p class="confirm-copy">Start building your vault! Keep track of your favorite kicks and manage your collection with ease.</p>

            <form class="shoe-form auth-form" action="/auth/sign-up" method="POST">
                <% if (typeof message !== 'undefined') { %>
                    <p class="form-error"><%= message %></p>
                <% } %>

                <div class="form-grid auth-grid">
                    <div class="field-group field-group-full">
                        <label for="username">Username</label>
                        <input type="text" name="username" id="username" required placeholder="Enter your username" />
                    </div>
                    <div class="field-group field-group-full">
                        <label for="password">Password</label>
                        <input type="password" name="password" id="password" required placeholder="Enter your password" />
                    </div>
                    <div class="field-group field-group-full">
                        <label for="confirmPassword">Confirm Password</label>
                        <input type="password" name="confirmPassword" id="confirmPassword" required placeholder="Confirm your password" />
                    </div>
                </div>

                <div class="shoe-actions">
                    <button class="btn-primary" type="submit">Sign Up</button>
                    <a class="btn-secondary" href="/auth/sign-in">Already have an account?</a>
                </div>
            </form>
        </section>
    </main>
</body>

</html>